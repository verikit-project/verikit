{
  "name": "verikit",
  "version": "0.23.0",
  "private": true,
  "description": "A TypeScript resource framework.",
  "scripts": {
    "prepack": "node -e \"console.error('Workspace root is private; publish packages instead.'); process.exit(1)\"",
    "vsync": "node script/sync-version.js",
    "build": "pnpm -r --filter @verikit/* build",
    "test": "pnpm -r --filter @verikit/* test",
    "test:coverage": "pnpm -r --stream --filter @verikit/* test:coverage",
    "typecheck": "tsc -b",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "docs:api": "pnpm build && typedoc --options typedoc.core.json && typedoc --options typedoc.runtime.json && typedoc --options typedoc.react.json && typedoc --options typedoc.server.json && typedoc --options typedoc.drizzle.json && typedoc --options typedoc.prisma.json && typedoc --options typedoc.client.json",
    "smoke-test": "node script/smoke-test.js",
    "verify": "pnpm typecheck && pnpm lint && pnpm build &&  pnpm format:check && pnpm test:coverage && pnpm test"
  },
  "keywords": [
    "typescript",
    "resource",
    "schema",
    "resource-model",
    "metadata",
    "builder",
    "validation",
    "framework"
  ],
  "author": "Chidi E <hi@ceeso.dev>",
  "license": "MIT",
  "devEngines": {
    "packageManager": {
      "name": "pnpm",
      "version": ">=11 <12",
      "onFail": "warn"
    }
  },
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.7.0",
    "eslint-config-prettier": "^10.1.8",
    "globals": "^17.7.0",
    "prettier": "^3.9.5",
    "shadcn": "^4.14.1",
    "typedoc": "^0.28.20",
    "typedoc-plugin-markdown": "^4.12.0",
    "typescript": "^6.0.3",
    "typescript-eslint": "^8.63.0"
  }
}
